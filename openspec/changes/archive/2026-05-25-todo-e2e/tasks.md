## 1. Instrumentação de Testes no React (data-testid)

- [x] 1.1 Adicionar atributos `data-testid` no `TodoForm.tsx` (input, select, botão, erro)
- [x] 1.2 Adicionar atributos `data-testid` no `TodoFilter.tsx` (botões de filtro)
- [x] 1.3 Adicionar atributos `data-testid` no `TodoItem.tsx` (checkbox, título, botões de ação e campos de edição inline)
- [x] 1.4 Adicionar atributos `data-testid` no `TodoList.tsx` (contêiner da lista)

## 2. Configuração e Dependências

- [x] 2.1 Instalar o Cypress com `npm install -D cypress`
- [x] 2.2 Configurar o Cypress em `cypress.config.ts` com a URL do localhost
- [x] 2.3 Adicionar scripts do Cypress no `package.json`

## 3. Implementação dos Testes E2E

- [x] 3.1 Criar o arquivo de teste `cypress/e2e/todo.cy.ts` com os 8 cenários de testes unitários E2E
