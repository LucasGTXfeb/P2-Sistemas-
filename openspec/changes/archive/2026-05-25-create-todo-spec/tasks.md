## 1. Configuração Inicial

- [x] 1.1 Inicializar o projeto usando o Vite com o template `react-ts` na pasta atual
- [x] 1.2 Instalar as dependências do projeto com `npm install`
- [x] 1.3 Criar e configurar as variáveis CSS de design no arquivo de estilos globais

## 2. Implementação dos Componentes e Estado

- [x] 2.1 Definir o tipo/interface `Todo` com os campos `id`, `title`, `completed` e `createdAt`
- [x] 2.2 Criar o componente `TodoItem.tsx` para exibição e alternância de estado de tarefas individuais
- [x] 2.3 Criar o componente `TodoForm.tsx` para manipulação de cadastro e validação
- [x] 2.4 Criar o componente `TodoList.tsx` para encapsular a lista de tarefas
- [x] 2.5 Atualizar o `App.tsx` para gerenciar o estado centralizado, integrando o `localStorage`
